#include<stdio.h>

int main()
{
    char str[100];
    int strlen(const char*str);
    int z;
    printf("enter the 1st string :");
    gets(str);
    z=strlen(str);
    printf("the length of string is :%d ",z);
    return 0;
}
